# 📝 To-Do List — React SPA

<img width="1231" height="844" alt="image" src="https://github.com/user-attachments/assets/4b758cde-278f-4f11-90bf-ab80a5af1e72" />


## 📘 Обзор Проекта

Это приложение **«Список Задач» (To-Do List)**, реализованное на **React** с использованием встроенного **JavaScript (JSX)** и **CSS** для создания приятного и функционального пользовательского интерфейса.  

🎯 **Основная цель работы** — освоить базовые принципы React:
- использование состояния (`useState`);
- построение компонентной структуры;
- передача данных между компонентами через `props`.


## 🎨 Ключевые Изменения и Дизайн

В проект были внесены изменения, улучшающие внешний вид и взаимодействие пользователя с приложением:

- 🌈 **Градиентный фон:** плавный переход от синего (`#4a76a8`) к нежно-телесному (`#f2d9c1`);
- 🪟 **Стиль контейнера:** основной блок (`#root`) имеет полупрозрачный фон и эффект размытия (`backdrop-filter`);
- 🔘 **Стили кнопок:** кнопки оформлены в синем цвете с тенями и эффектом нажатия (`:active`);
- ⏳ **Анимация загрузки (Spinner):** добавлена на кнопку **«Добавить»** — имитирует асинхронную работу с помощью `setTimeout`, временно блокируя ввод;
- ✨ **Анимация задач:** новые задачи появляются с плавным эффектом (`fadeInTask`).


## 🧩 Структура Кода (React Компоненты)

Проект состоит из трёх основных функциональных компонентов:

### ⚙️ `App`
Главный компонент приложения.  
- Хранит состояние (`useState`) со списком задач (`tasks`);
- Реализует функции **добавления** (`addTask`) и **удаления** (`removeTask`) задач;
- Передаёт данные в дочерние компоненты через `props`.

### 🖊️ `TaskForm`
Компонент для ввода новой задачи.  
- Содержит поле `input` и кнопку **«Добавить»**;
- Передаёт введённый текст в функцию `addTask` родительского компонента;
- Управляет локальным состоянием и анимацией загрузки (`isLoading`).

### 📋 `TaskList`
Компонент для отображения списка задач.  
- Получает массив `tasks` и функцию `removeTask` через `props`;
- Использует метод `map()` для генерации элементов списка;
- Каждая задача отображается с кнопкой **«Удалить»**.


## 💡 Вывод: Чему Я Научился

В ходе разработки были освоены ключевые навыки работы с React:

- 🔄 **Управление состоянием:** применение хука `useState` для отслеживания списка задач и состояний загрузки;
- 🧱 **Архитектура компонентов:** построение приложения из независимых, переиспользуемых блоков;
- 🔗 **Передача данных (`props`):** взаимодействие между родительскими и дочерними компонентами;
- 💫 **Пользовательский опыт (UX):** использование CSS-анимаций для плавных эффектов и визуальной обратной связи.


📦 **Технологии:**  
React • JSX • CSS • JavaScript (ES6)
