Обзор Проекта
​Это приложение "Список Задач" (To-Do List), реализованное на React с использованием встроенного JavaScript (JSX) и стилей CSS для создания уникального и приятного пользовательского интерфейса. Основная цель работы — освоить базовые принципы React: состояние (useState), компоненты и передачу данных (props).

​🎨 Ключевые Изменения и Дизайн
​В проект были внесены значительные изменения, касающиеся оформления и анимации:
​Градиентный Фон: Фон страницы оформлен приятным градиентом от синего (#4a76a8) к нежно-телесному (#f2d9c1).
​Стиль Контейнера: Основной контейнер (#root) сделан полупрозрачным и имеет эффект легкого размытия (backdrop-filter).
​Стили Кнопок: Кнопки стилизованы под основной синий цвет с тенью и эффектом нажатия (:active).
​Анимация Загрузки: Добавлена анимация загрузки (Spinner) на кнопку "Добавить". Спиннер имитирует асинхронную работу (setTimeout), отключая кнопку и поле ввода на время "добавления".
​Анимация Задачи: Новые задачи появляются в списке с эффектом плавного появления (fadeInTask CSS animation).
​🧩 Структура Кода (React Компоненты)
​Код разделен на три основных функциональных компонента:

App Главный компонент. Содержит состояние (useState) для хранения массива задач (tasks). Отвечает за логику добавления (addTask) и удаления (removeTask) задач.
TaskForm Компонент ввода. Содержит поле input и кнопку "Добавить". Передает введенный текст в функцию addTask родительского компонента App через props. Управляет состоянием загрузки (isLoading).
TaskList Компонент отображения списка. Получает массив tasks и функцию removeTask через props. Использует метод map() для рендеринга каждого элемента в виде <li> с кнопкой "Удалить".

💡 Вывод: Чему Научился
​В ходе работы над проектом были освоены следующие ключевые навыки:
​Управление Состоянием (State Management): Использование хука useState для отслеживания списка задач и состояния загрузки.
​Архитектура Компонентов: Создание структурированного приложения путем разделения интерфейса на независимые, многоразовые компоненты.
​Передача Данных (Props): Эффективная передача функций и данных между родительскими и дочерними компонентами.
​Пользовательский Опыт (UX): Интеграция CSS-анимаций для улучшения взаимодействия с пользователем (плавное появление задач, индикатор загрузки).
